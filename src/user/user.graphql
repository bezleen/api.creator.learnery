scalar DateTime

type User {
  id: String!
  email: String
  firstName: String
  lastName: String
  avatar: String
  refreshToken: String
  createdAt: DateTime
  updatedAt: DateTime
  password: String
}

type Query {
  me: User!
}
