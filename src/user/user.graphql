scalar DateTime

type User {
  id: String!
  email: String
  firstName: String
  lastName: String
  avatar: String
  refreshToken: String
  createdAt: DateTime
  updatedAt: DateTime
}

type Query {
  me: User!
}
